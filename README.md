# GameMaker

VB.NET 3.5 working copy whose `Helper` library (`RootNamespace` GameMaker, assembly Helper) stores named numeric and string arrays (`Arrays`) and maps GameMaker-style colour names such as c_aqua/c_red to integer values (`Constants`). The `Test` WinForms project (`Test/Form1.vb`) constructs `GameMaker.Arrays` on load and Debug.Prints set/get/size for numeric and string arrays. Open `GameMaker.sln` (VS 2008). This is a historical working copy from Dave Robinson / VaderConsulting.

**Source last updated:** 2009-09-08  
**Language:** VB.NET  
**Target:** v3.5  
**Output:** Library, WinExe

## What it is

VB.NET 3.5 working copy whose `Helper` library (`RootNamespace` GameMaker, assembly Helper) stores named numeric and string arrays (`Arrays`) and maps GameMaker-style colour names such as c_aqua/c_red to integer values (`Constants`). The `Test` WinForms project (`Test/Form1.vb`) constructs `GameMaker.Arrays` on load and Debug.Prints set/get/size for numeric and string arrays. Open `GameMaker.sln` (VS 2008). This is a historical working copy from Dave Robinson / VaderConsulting.

## Solution structure

| Project | Language | Path |
|---------|----------|------|
| `Test` | VB.NET | `Test/Test.vbproj` |
| `Helper` | VB.NET | `Helper/Helper.vbproj` |

## How to open

Open `GameMaker.sln` in Visual Studio.

## Attribution and provenance

- **Assembly company:** Microsoft
- **Assembly copyright:** Copyright © Microsoft 2009

## License

MIT. See `LICENSE`.
